#include <algorithm>
#include <vector>
#include <iostream>

class Point{
  public:
    Point() {
    }
    Point(int x_coord, int y_coord) {
      x_coord_ = x_coord;
      y_coord_ = y_coord;
    }
    bool operator < (const &Point) {

    }
}
